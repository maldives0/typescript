{
  "compilerOptions": {
    "strict": true,
    "lib": ["es2020"],
    "moduleResolution": "node",
    "typeRoots": ["./types"]//가져올 타이핑을 지정해준다. 이 부분이 없으면 기본적으로 node_modules의 @type안의 모든 ts를 가져온다
  },
  // "files":["./index.ts","./types/index.ts"]// compile할 파일을 지정해준다
}